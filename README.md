# Dotfiles

This is a dotfile store for my systems; so that I can easily copy in configuration files into new systems when I deem it necessary.

## Installation Instructions

Run `./install.sh` to install necessary packages w/brew & copy configuration files into `.config/`.
